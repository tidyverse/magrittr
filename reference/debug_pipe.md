# Debugging function for magrittr pipelines.

This function is a wrapper around `browser`, which makes it easier to
debug at certain places in a magrittr pipe chain.

## Usage

``` r
debug_pipe(x)
```

## Arguments

- x:

  a value

## Value

x
